{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "https://github.com/amtrack/sfdx-browserforce-plugin/src/plugins/scheduled-batches/schema.json",
  "title": "Scheduled Batches",
  "description": "Schedule batch jobs by checking the Scheduled checkboxes on the Batch Job Scheduler Configuration page. When jobNames is omitted, all checkboxes are checked.",
  "type": "object",
  "properties": {
    "jobNames": {
      "title": "Job Names",
      "description": "Optional. List of batch job names to schedule (matches the Job Name column in the table). When omitted, all checkboxes are checked.",
      "type": "array",
      "items": { "type": "string" }
    }
  }
}
